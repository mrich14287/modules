/*
resource "oci_file_storage_mount_target" "mount_target" {
  #Required
  availability_domain = var.mount_target_availability_domain
  compartment_id = var.project_comparment_id
  subnet_id = var.subnet_id

  #Optional
  defined_tags = var.defined_tags
  display_name = var.mt_display_name
  freeform_tags = var.freeform_tags
  hostname_label = var.mt_hostname_label
  nsg_ids = var.mtt_nsg_ids
} */
